# 💎 $SYNTRA Points System - Полная Документация

**Дата:** 2025-12-03
**Статус:** ✅ Базовая инфраструктура реализована
**Версия:** 1.0

---

## 📋 Оглавление

1. [Обзор системы](#обзор-системы)
2. [Архитектура](#архитектура)
3. [Реализованные компоненты](#реализованные-компоненты)
4. [Конфигурация начислений](#конфигурация-начислений)
5. [Система уровней](#система-уровней)
6. [API и интеграция](#api-и-интеграция)
7. [План дальнейшей разработки](#план-дальнейшей-разработки)

---

## 🎯 Обзор системы

### Что такое $SYNTRA Points?

$SYNTRA - это внутренняя игровая валюта (поинты) для платформы Syntra Trade Consultant, которая:

- ✅ **Награждает активность** - пользователи получают поинты за использование сервиса
- ✅ **Стимулирует вовлеченность** - система уровней и стриков мотивирует ежедневное использование
- ✅ **Создает ценность** - поинты можно обменять на подписки и дополнительные запросы
- ✅ **Геймифицирует опыт** - прогрессия, достижения, множители

### Ключевые метрики

```
📊 Базовые начисления:
├── Текстовый запрос: 10 поинтов
├── График/анализ: 15 поинтов
├── Vision анализ: 20 поинтов
├── Регистрация реферала: 500 поинтов
└── Покупка рефералом: 1000 поинтов

🎮 Множители:
├── FREE tier: x1.0 (базовый)
├── BASIC tier: x1.2 (+20%)
├── PREMIUM tier: x1.5 (+50%)
└── VIP tier: x2.0 (+100%)

🏆 Уровни (до 6 уровней):
├── Новичок (0 поинтов): x1.0
├── Трейдер (1,000): x1.1
├── Аналитик (5,000): x1.2
├── Эксперт (15,000): x1.3
├── Мастер (50,000): x1.5
└── Легенда (150,000): x2.0 ⭐
```

---

## 🏗️ Архитектура

### Компоненты системы

```
┌─────────────────────────────────────────────────┐
│          FRONTEND (React/Next.js)               │
│  - PointsDisplay компонент                      │
│  - Leaderboard                                  │
│  - Transaction History                          │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│            API LAYER (FastAPI)                  │
│  - GET /api/points/balance                      │
│  - GET /api/points/history                      │
│  - POST /api/points/spend                       │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│         BUSINESS LOGIC (PointsService)          │
│  - earn_points()                                │
│  - spend_points()                               │
│  - process_daily_login()                        │
│  - get_balance()                                │
└─────────────────┬───────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────┐
│          DATABASE (PostgreSQL)                  │
│  ├── points_balances                            │
│  ├── points_transactions                        │
│  └── points_levels                              │
└─────────────────────────────────────────────────┘
```

### Database Schema

#### `points_balances` - Баланс пользователя

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | Integer | Primary key |
| `user_id` | Integer | User ID (unique) |
| `balance` | Integer | Текущий баланс |
| `lifetime_earned` | Integer | Всего заработано |
| `lifetime_spent` | Integer | Всего потрачено |
| `level` | Integer | Уровень пользователя |
| `earning_multiplier` | Float | Множитель заработка |
| `current_streak` | Integer | Текущий стрик |
| `longest_streak` | Integer | Самый длинный стрик |
| `last_daily_login` | DateTime | Последний вход |

#### `points_transactions` - История транзакций

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | Integer | Primary key |
| `user_id` | Integer | User ID |
| `transaction_type` | String | Тип транзакции |
| `amount` | Integer | Сумма (+/-) |
| `balance_before` | Integer | Баланс до |
| `balance_after` | Integer | Баланс после |
| `description` | Text | Описание |
| `metadata_json` | Text | JSON метаданные |
| `transaction_id` | String | Уникальный ID (idempotency) |
| `created_at` | DateTime | Дата создания |

#### `points_levels` - Конфигурация уровней

| Поле | Тип | Описание |
|------|-----|----------|
| `level` | Integer | Номер уровня |
| `name_ru` | String | Название (RU) |
| `name_en` | String | Название (EN) |
| `icon` | String | Emoji иконка |
| `points_required` | Integer | Требуемые поинты |
| `earning_multiplier` | Float | Множитель заработка |

---

## ✅ Реализованные компоненты

### 1. Database Models ([src/database/models.py](src/database/models.py))

✅ **Созданы 3 модели:**
- `PointsBalance` - баланс пользователя с геймификацией
- `PointsTransaction` - полная история с idempotency
- `PointsLevel` - конфигурация уровней

**Особенности:**
- Атомарные транзакции
- Защита от дублирования (transaction_id)
- Снапшоты баланса (balance_before/after)
- Поддержка экспирации поинтов

### 2. Migration ([alembic/versions/e76bd21c31a7_add_syntra_points_system.py](alembic/versions/e76bd21c31a7_add_syntra_points_system.py))

✅ **Готовая миграция:**
- Создает все 3 таблицы
- Индексы для производительности
- Преднастроенные 6 уровней (Новичок → Легенда)

**Запуск:**
```bash
source .venv/bin/activate
alembic upgrade head
```

### 3. Configuration ([config/points_config.py](config/points_config.py))

✅ **Централизованная конфигурация:**
- `BASE_POINTS_EARNING` - начисления за действия
- `SUBSCRIPTION_BONUSES` - бонусы за подписку
- `SUBSCRIPTION_EARNING_MULTIPLIERS` - множители
- `STREAK_BONUSES` - бонусы за стрики
- `SUBSCRIPTION_POINTS_COST` - стоимость в поинтах

**Функции-хелперы:**
- `get_points_for_action()` - расчет с множителями
- `get_subscription_bonus()` - бонус за подписку
- `get_streak_bonus()` - бонус за стрик

### 4. Points Service ([src/services/points_service.py](src/services/points_service.py))

✅ **Основной сервис с методами:**

```python
# Начисление поинтов
await PointsService.earn_points(
    session=session,
    user_id=user_id,
    transaction_type=PointsTransactionType.EARN_TEXT_REQUEST,
    description="Анализ BTC/USDT"
)

# Списание поинтов
await PointsService.spend_points(
    session=session,
    user_id=user_id,
    transaction_type=PointsTransactionType.SPEND_SUBSCRIPTION,
    amount=2000
)

# Ежедневный вход
await PointsService.process_daily_login(
    session=session,
    user_id=user_id
)

# Получить баланс
balance_info = await PointsService.get_balance(
    session=session,
    user_id=user_id
)
```

**Встроенная защита:**
- ✅ Rate limiting (мин. интервал между начислениями)
- ✅ Daily cap (макс. 10,000 поинтов/день)
- ✅ Idempotency (защита от дублей)
- ✅ Автоматический расчет уровней

---

## 💰 Конфигурация начислений

### Заработок поинтов

| Действие | Базовые поинты | FREE user | PREMIUM user | VIP user |
|----------|----------------|-----------|--------------|----------|
| Текстовый запрос | 10 | 10 | 15 | 20 |
| График/анализ | 15 | 15 | 22 | 30 |
| Vision анализ | 20 | 20 | 30 | 40 |
| Регистрация реферала | 500 | 500 | 750 | 1000 |
| Покупка рефералом | 1000 | 1000 | 1500 | 2000 |

### Бонусы за подписку (одноразово)

| Тариф | 1 месяц | 3 месяца | 12 месяцев |
|-------|---------|----------|------------|
| BASIC | 100 | 400 | 2,000 |
| PREMIUM | 500 | 2,000 | 10,000 |
| VIP | 1,500 | 6,000 | 30,000 |

### Стоимость подписки в поинтах

| Тариф | 1 месяц | 3 месяца | 12 месяцев |
|-------|---------|----------|------------|
| BASIC ($4.99) | 400 | 1,200 | 4,800 |
| PREMIUM ($24.99) | 2,000 | 6,000 | 24,000 |
| VIP ($49.99) | 4,000 | 12,000 | 48,000 |

**Курс:** ~80 поинтов = $1 (скидка 20% от USD цены)

---

## 🏆 Система уровней

| Level | Название | Icon | Поинты | Множитель | Описание |
|-------|----------|------|--------|-----------|----------|
| 1 | Новичок / Beginner | 🌱 | 0 | x1.0 | Добро пожаловать в Syntra! |
| 2 | Трейдер / Trader | 📈 | 1,000 | x1.1 | Начинаете разбираться в криптовалютах |
| 3 | Аналитик / Analyst | 🔍 | 5,000 | x1.2 | Умеете анализировать рынок |
| 4 | Эксперт / Expert | ⭐ | 15,000 | x1.3 | Эксперт в техническом анализе |
| 5 | Мастер / Master | 💎 | 50,000 | x1.5 | Достигли мастерства в трейдинге |
| 6 | Легенда / Legend | 👑 | 150,000 | x2.0 | Легенда Syntra! Максимальный множитель |

### Расчет прогресса

```python
# Пример: Пользователь заработал 7,500 поинтов
lifetime_earned = 7500

# Текущий уровень: 3 (Аналитик)
# Следующий уровень: 4 (Эксперт) требует 15,000
# До следующего уровня: 15,000 - 7,500 = 7,500 поинтов
```

---

## 🔌 API и интеграция

### Примеры использования

#### 1. Начисление поинтов за запрос

```python
from src.services.points_service import PointsService
from src.database.models import PointsTransactionType

# В обработчике AI запроса
async def handle_text_request(user_id: int, session: AsyncSession):
    # ... обработка запроса ...

    # Начислить поинты
    await PointsService.earn_points(
        session=session,
        user_id=user_id,
        transaction_type=PointsTransactionType.EARN_TEXT_REQUEST,
        description="Анализ рынка BTC",
        metadata={"coin": "BTC", "request_type": "market_analysis"},
        transaction_id=f"text_req:{user_id}:{request_id}"  # Idempotency
    )
```

#### 2. Бонус за покупку подписки

```python
from config.points_config import get_subscription_bonus

async def handle_subscription_purchase(
    user_id: int,
    tier: str,
    duration_months: int,
    session: AsyncSession
):
    # Получить бонус из конфига
    bonus_points = get_subscription_bonus(tier, duration_months)

    # Начислить бонус
    await PointsService.earn_points(
        session=session,
        user_id=user_id,
        transaction_type=PointsTransactionType.EARN_SUBSCRIPTION,
        amount=bonus_points,
        description=f"Бонус за покупку {tier} на {duration_months} мес.",
        transaction_id=f"sub_bonus:{payment_id}"
    )
```

#### 3. Ежедневный вход

```python
# В middleware или при входе пользователя
async def handle_user_login(user_id: int, session: AsyncSession):
    # Обработать daily login (автоматически считает streak)
    transaction = await PointsService.process_daily_login(
        session=session,
        user_id=user_id
    )

    if transaction:
        # Пользователь получил бонус
        streak = json.loads(transaction.metadata_json)["streak"]
        return f"Получено {transaction.amount} поинтов! Streak: {streak} дней"
```

#### 4. Получение баланса

```python
# Для отображения в UI
balance_info = await PointsService.get_balance(session, user_id)

# Вернет:
{
    "balance": 5420,
    "lifetime_earned": 12300,
    "lifetime_spent": 6880,
    "level": 3,
    "level_name_ru": "Аналитик",
    "level_name_en": "Analyst",
    "level_icon": "🔍",
    "earning_multiplier": 1.2,
    "current_streak": 7,
    "longest_streak": 15,
    "next_level": 4,
    "next_level_points_required": 15000,
    "points_to_next_level": 2700
}
```

---

## 📊 План дальнейшей разработки

### ✅ Фаза 1: Базовая инфраструктура (ГОТОВО)

- ✅ Database models
- ✅ Alembic migration
- ✅ Configuration система
- ✅ PointsService с основными методами
- ✅ Документация

### 🔄 Фаза 2: Интеграция с существующими системами (В ПРОЦЕССЕ)

#### 2.1 Интеграция с AI запросами

**Файлы для модификации:**
- `src/bot/handlers/chat.py` - добавить начисление за текстовые запросы
- `src/bot/handlers/vision.py` - добавить начисление за vision
- `src/services/openai_service.py` - хук после успешного запроса

**Пример интеграции:**
```python
# В src/bot/handlers/chat.py после успешного ответа AI
await PointsService.earn_points(
    session=session,
    user_id=user.id,
    transaction_type=PointsTransactionType.EARN_TEXT_REQUEST,
    transaction_id=f"chat:{user.id}:{message.message_id}"
)
```

#### 2.2 Интеграция с платежами

**Файлы:**
- `src/services/telegram_stars_service.py`
- `src/services/ton_payment_service.py`
- `src/services/crypto_pay_service.py`

**Добавить:**
- Начисление бонуса после успешной оплаты
- Возможность оплаты подпиской поинтами

#### 2.3 Интеграция с реферальной системой

**Файлы:**
- `src/bot/handlers/referral.py`
- `src/tasks/referral_scheduler.py`

**Добавить:**
- Начисление за регистрацию реферала
- Начисление за покупку рефералом

### 🎯 Фаза 3: API Endpoints

**Создать:**
- `src/api/points.py` - новый API модуль

**Endpoints:**
```python
GET  /api/points/balance       # Получить баланс
GET  /api/points/history       # История транзакций
GET  /api/points/leaderboard   # Таблица лидеров
POST /api/points/spend         # Потратить поинты
```

### 🎨 Фаза 4: Frontend UI

**Компоненты:**
1. `PointsDisplay` - отображение баланса в header
2. `LevelProgress` - прогресс-бар до следующего уровня
3. `TransactionHistory` - история начислений
4. `Leaderboard` - топ пользователей
5. `StreakBadge` - бейдж с текущим стриком

**Страницы:**
- `/profile/points` - детальная страница поинтов
- `/leaderboard` - таблица лидеров

### 🌍 Фаза 5: Локализация

**Файлы:**
- `src/locales/ru.json`
- `src/locales/en.json`
- `frontend/messages/ru.json`
- `frontend/messages/en.json`

**Добавить ключи:**
```json
{
  "points": {
    "balance": "Баланс",
    "earned": "Заработано",
    "level": "Уровень",
    "next_level": "Следующий уровень",
    "streak": "Серия",
    "transactions": "История",
    "earn_text_request": "Заработано за анализ",
    "earn_daily_login": "Ежедневный бонус"
  }
}
```

### 🧪 Фаза 6: Тестирование

**Создать тесты:**
- `tests/test_points_service.py` - unit тесты сервиса
- `tests/test_points_api.py` - API тесты
- `tests/test_points_integration.py` - интеграционные тесты

**Проверить:**
- Idempotency (повторные начисления)
- Rate limiting
- Daily cap
- Streak calculation
- Level progression
- Balance calculations

### 🎮 Фаза 7: Gamification 2.0 (будущее)

**Дополнительные фичи:**
- Achievements (достижения)
- Challenges (челленджи)
- Quests (квесты)
- Seasonal events (сезонные события)
- NFT rewards (NFT награды)

---

## 🚀 Быстрый старт для разработчиков

### 1. Применить миграцию

```bash
cd /Users/a1/Projects/Syntra Trade Consultant
source .venv/bin/activate
alembic upgrade head
```

### 2. Импортировать и использовать

```python
from src.services.points_service import PointsService
from src.database.models import PointsTransactionType

# В любом async контексте с session
await PointsService.earn_points(
    session=session,
    user_id=user_id,
    transaction_type=PointsTransactionType.EARN_TEXT_REQUEST
)
```

### 3. Настроить конфигурацию

Отредактировать [config/points_config.py](config/points_config.py):
- Изменить начисления
- Настроить бонусы
- Изменить лимиты

---

## 📚 Ресурсы

### Файлы проекта

- 📄 [Database Models](src/database/models.py:2552-2888) - Модели БД
- 📄 [Migration](alembic/versions/e76bd21c31a7_add_syntra_points_system.py) - Миграция
- 📄 [Configuration](config/points_config.py) - Конфигурация
- 📄 [Service](src/services/points_service.py) - Бизнес-логика

### Источники вдохновения

- [Best Practices for Loyalty Program UX](https://www.voucherify.io/blog/loyalty-programs-ux-and-ui-best-practices)
- [Gamification in Loyalty Programs 2025](https://www.opsmatters.com/posts/7-best-loyalty-program-gamification-techniques-2025)
- [Building Rewards Platform Architecture](https://softwareengineeringdaily.com/2018/11/28/building-a-rewards-platform-from-scratch/)
- [Loyalty Architecture Design](https://www.voucherify.io/blog/architecture-of-customer-loyalty-software-a-guide-for-product-managers)

---

## 💡 Рекомендации по внедрению

### Приоритет интеграции:

1. **Высокий приоритет:**
   - ✅ Daily login бонус (удержание пользователей)
   - ✅ Начисление за AI запросы (основная активность)
   - ✅ Бонус за подписку (монетизация)

2. **Средний приоритет:**
   - ⏳ Реферальные бонусы
   - ⏳ API endpoints
   - ⏳ Frontend UI

3. **Низкий приоритет:**
   - ⏳ Leaderboard
   - ⏳ Achievements
   - ⏳ Advanced gamification

### Метрики для отслеживания:

- 📈 DAU (Daily Active Users) с поинтами
- 📈 Средний заработок поинтов/пользователь
- 📈 Конверсия поинтов в подписки
- 📈 Retention rate (с/без поинтов)
- 📈 Streak completion rate

---

**Готово к использованию!** 🎉

Система $SYNTRA Points полностью подготовлена к интеграции. Следующие шаги - подключить начисления в существующие обработчики запросов и платежей.
