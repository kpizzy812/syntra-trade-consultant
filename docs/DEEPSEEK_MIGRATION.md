# DeepSeek Integration Guide

## Обзор

Система Syntra Trade Consultant теперь поддерживает переключение между OpenAI и DeepSeek AI провайдерами. DeepSeek предлагает значительно более низкие цены при сохранении высокого качества ответов.

## Сравнение стоимости

| Провайдер | Input (per 1M tokens) | Output (per 1M tokens) | Экономия vs GPT-4o |
|-----------|----------------------|------------------------|-------------------|
| **GPT-4o** | $2.50 | $10.00 | - |
| **GPT-4o-mini** | $0.15 | $0.60 | - |
| **DeepSeek** | $0.28 | $0.42 | **~9x дешевле input, ~24x дешевле output** |
| **DeepSeek (cached)** | $0.028 | $0.42 | **~89x дешевле input!** |

### Выводы

- DeepSeek **в 5-9 раз дешевле GPT-4o**
- DeepSeek дороже GPT-4o-mini по input (~1.9x), но **дешевле по output** (~1.4x)
- С кешированием промптов DeepSeek обгоняет все модели по цене

## Быстрый старт

### 1. Получите API ключ DeepSeek

1. Зарегистрируйтесь: https://platform.deepseek.com/sign-up
2. Создайте API ключ: https://platform.deepseek.com/api-keys

### 2. Настройте .env файл

```bash
# Переключите провайдера на DeepSeek
AI_PROVIDER=deepseek

# Добавьте DeepSeek API ключ
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# OpenAI ключ всё равно нужен для Vision API
OPENAI_API_KEY=sk-your-openai-api-key
```

### 3. Перезапустите бота

```bash
# Остановите текущий процесс
./manage.sh stop

# Запустите с новой конфигурацией
./manage.sh start
```

## Архитектура

### Гибридная система

Система автоматически использует правильного провайдера в зависимости от задачи:

| Задача | Провайдер | Причина |
|--------|-----------|---------|
| **Текстовый чат** | AI_PROVIDER (OpenAI или DeepSeek) | Настраивается через `.env` |
| **Vision API** | OpenAI (всегда) | DeepSeek не поддерживает Vision |
| **Function Calling** | AI_PROVIDER | Оба провайдера поддерживают |

### Внутренние детали

```python
# config/config.py
AI_PROVIDER = "deepseek"  # или "openai"
DEEPSEEK_API_KEY = "sk-..."
DEEPSEEK_BASE_URL = "https://api.deepseek.com"

# src/services/openai_service.py
class OpenAIService:
    def __init__(self):
        if AI_PROVIDER == "deepseek":
            # Для чата - DeepSeek
            self.client = AsyncOpenAI(
                api_key=DEEPSEEK_API_KEY,
                base_url=DEEPSEEK_BASE_URL
            )
            # Для Vision - всегда OpenAI
            self.vision_client = AsyncOpenAI(api_key=OPENAI_API_KEY)
        else:
            # OpenAI для всего
            self.client = AsyncOpenAI(api_key=OPENAI_API_KEY)
            self.vision_client = self.client
```

## Модели

### DeepSeek модели

| Модель | Описание | Использование |
|--------|----------|--------------|
| `deepseek-chat` | Основная модель (аналог GPT-4o) | Все текстовые задачи |
| `deepseek-reasoner` | Chain-of-thought модель (аналог o1) | Сложная логика, математика |

**Примечание:** DeepSeek не имеет "mini" версии. `deepseek-chat` используется для всех запросов.

### OpenAI модели (для сравнения)

| Модель | Использование |
|--------|--------------|
| `gpt-4o` | Сложные задачи, Vision |
| `gpt-4o-mini` | Простые задачи |

## Автоматический роутинг моделей

Система автоматически выбирает модель на основе:

1. **Ключевые слова** (анализ, стратегия, прогноз) → главная модель
2. **Количество токенов** (>1500) → главная модель
3. **Иначе** → экономичная модель

```python
# Для DeepSeek
if complex_keywords or tokens > 1500:
    model = "deepseek-chat"  # главная
else:
    model = "deepseek-chat"  # та же (нет mini)

# Для OpenAI
if complex_keywords or tokens > 1500:
    model = "gpt-4o"  # главная
else:
    model = "gpt-4o-mini"  # экономичная
```

## Особенности DeepSeek

### Кеширование промптов

DeepSeek автоматически кеширует system prompts:

- **Cache hit**: $0.028 per 1M tokens (в 10 раз дешевле!)
- **Cache miss**: $0.28 per 1M tokens
- **Cache TTL**: ~5-10 минут

Система Syntra уже оптимизирована для кеширования:
- System prompt компактный (~1500 chars)
- Структура сообщений оптимальна

### Ограничения DeepSeek

❌ **Не поддерживается:**
- Vision API (анализ изображений)
- Whisper (распознавание речи)

✅ **Поддерживается:**
- Function Calling (crypto tools)
- Streaming responses
- 128K context window
- JSON output mode

## Тестирование

### Проверка переключения провайдера

```bash
# 1. Запустите бота с DeepSeek
AI_PROVIDER=deepseek ./manage.sh start

# 2. Отправьте текстовый запрос
# Ожидается: DeepSeek обработает запрос

# 3. Отправьте изображение
# Ожидается: OpenAI обработает (Vision API)

# 4. Проверьте логи
tail -f logs/bot.log | grep "Initializing"
# Должно показать:
# 🔧 Initializing DeepSeek client for chat
# 🔧 Initializing OpenAI client for Vision
```

### A/B тестирование качества

```bash
# Тест 1: OpenAI
AI_PROVIDER=openai ./manage.sh start
# Задайте вопрос: "Проанализируй BTC"

# Тест 2: DeepSeek
AI_PROVIDER=deepseek ./manage.sh start
# Задайте тот же вопрос

# Сравните качество ответов
```

## Мониторинг затрат

Система автоматически отслеживает затраты в БД:

```sql
SELECT
    service,
    model,
    SUM(cost) as total_cost,
    SUM(tokens) as total_tokens
FROM cost_tracking
WHERE created_at > NOW() - INTERVAL '7 days'
GROUP BY service, model
ORDER BY total_cost DESC;
```

Пример вывода:
```
service          | model         | total_cost | total_tokens
-----------------|---------------|------------|-------------
openai           | gpt-4o        | 12.45      | 3,245,000
openai_vision    | gpt-4o        | 8.32       | 1,890,000
openai_two_step  | mini+4o       | 5.67       | 4,120,000
deepseek         | deepseek-chat | 0.89       | 2,100,000  ← В 14x дешевле!
```

## Откат на OpenAI

Если возникли проблемы с DeepSeek:

```bash
# 1. Измените .env
AI_PROVIDER=openai

# 2. Перезапустите бота
./manage.sh restart

# Система мгновенно переключится на OpenAI
```

## FAQ

### Q: Нужен ли OpenAI ключ при использовании DeepSeek?
**A:** Да! OpenAI ключ нужен для Vision API (анализ графиков), который не поддерживается DeepSeek.

### Q: Можно ли использовать оба провайдера одновременно?
**A:** Нет, но система автоматически использует OpenAI для Vision, даже если `AI_PROVIDER=deepseek`.

### Q: Качество DeepSeek хуже OpenAI?
**A:** Субъективно. DeepSeek показывает хорошее качество на большинстве задач. Рекомендуется A/B тестирование.

### Q: Сколько я сэкономлю?
**A:** Зависит от нагрузки. В среднем:
- Если было 100% GPT-4o → экономия ~85-90%
- Если было 50% GPT-4o + 50% GPT-4o-mini → экономия ~40-60%

### Q: Как работает кеширование?
**A:** DeepSeek автоматически кеширует повторяющиеся части промпта (system prompt). Никакой дополнительной настройки не требуется.

### Q: Поддерживает ли DeepSeek Function Calling?
**A:** Да! `deepseek-chat` поддерживает Function Calling (crypto tools), но `deepseek-reasoner` не поддерживает.

## Рекомендации

### Для production

✅ **Используйте DeepSeek если:**
- Важна экономия бюджета
- Текстовые запросы преобладают над Vision
- Качество ответов приемлемо (проведите A/B тест)

❌ **Используйте OpenAI если:**
- Критичное качество ответов
- Много Vision запросов
- Бюджет не ограничен

### Оптимальная стратегия

```bash
# Для большинства пользователей (текст)
AI_PROVIDER=deepseek

# Vision автоматически использует OpenAI
# Экономия: 80-90% на текстовых запросах
```

## Поддержка

Проблемы с DeepSeek? Создайте issue с логами:

```bash
# Соберите логи
tail -n 100 logs/bot.log > deepseek_issue.log

# Добавьте в issue:
# - AI_PROVIDER настройка
# - Версия DeepSeek API
# - Текст ошибки
```

## История изменений

### v1.0.0 (2025-01-23)
- ✅ Добавлена поддержка DeepSeek
- ✅ Гибридная система (DeepSeek для chat, OpenAI для Vision)
- ✅ Автоматическое переключение провайдеров
- ✅ Tracking затрат для обоих провайдеров
- ✅ Документация и примеры

---

**Автор:** Syntra AI Team
**Дата:** 23 января 2025
**Версия:** 1.0.0
