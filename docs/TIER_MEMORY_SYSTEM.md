# Система памяти и контекста по тарифам

## Обзор

Реализована дифференцированная система памяти чата для разных тарифов подписки.

## Тарифные планы и память

### FREE Tier
- **Память чата:** ❌ НЕТ (0 сообщений)
- **Сохранение истории:** ❌ НЕТ (не сохраняется в БД)
- **Особенности:**
  - Каждый запрос независим
  - Промпт явно указывает на отсутствие памяти
  - Не может ссылаться на предыдущие сообщения
  - Подходит для быстрых разовых вопросов

### BASIC Tier
- **Память чата:** ✅ 5 сообщений
- **Сохранение истории:** ✅ ДА (сохраняется в БД)
- **Особенности:**
  - Ограниченная память последних 5 сообщений
  - Может продолжать короткие диалоги
  - Подходит для базового анализа с контекстом

### PREMIUM Tier
- **Память чата:** ✅ 10 сообщений
- **Сохранение истории:** ✅ ДА (сохраняется в БД)
- **Особенности:**
  - Хорошая память последних 10 сообщений
  - Может вести развернутые диалоги
  - Подходит для глубокого анализа

### VIP Tier
- **Память чата:** ✅ 50 сообщений
- **Сохранение истории:** ✅ ДА (сохраняется в БД)
- **Особенности:**
  - Максимальная память последних 50 сообщений
  - Может вести длительные диалоги
  - Подходит для профессионального использования

## Технические изменения

### 1. Конфигурация лимитов ([config/limits.py](../config/limits.py))

Добавлены новые параметры для каждого тарифа:

```python
"chat_history_messages": 0,   # Количество сообщений истории
"save_chat_history": False,   # Сохранять ли историю в БД
```

Добавлены новые функции:
- `get_chat_history_limit(tier)` - получить лимит истории для тарифа
- `should_save_chat_history(tier)` - проверить нужно ли сохранять историю

### 2. Промпты

#### FREE Tier ([config/prompts_free.py](../config/prompts_free.py))
```
# ВАЖНО: FREE ВЕРСИЯ БЕЗ ПАМЯТИ

⚠️ **КАЖДЫЙ ЗАПРОС НЕЗАВИСИМ** - у тебя НЕТ доступа к предыдущим сообщениям пользователя.
Не пытайся ссылаться на "предыдущий разговор" или "как я говорил ранее" - у тебя нет памяти.
Каждый ответ должен быть самодостаточным и полным.
```

#### Платные тарифы ([config/prompts.py](../config/prompts.py))
```
# КОНТЕКСТ И ПАМЯТЬ

✅ **У ТЕБЯ ЕСТЬ ПАМЯТЬ** - тебе доступна история чата с пользователем.

**ОБЯЗАТЕЛЬНО:**
- Обращай внимание на предыдущие сообщения пользователя
- Если пользователь продолжает разговор на ту же тему — продолжай диалог, ссылайся на контекст
- Если пользователь задаёт новый вопрос на другую тему — начинай новый анализ
- Используй информацию из истории для более персонализированных ответов
- Помни о предыдущих рекомендациях и упоминай их если релевантно
```

### 3. OpenAI Service ([src/services/openai_service.py](../src/services/openai_service.py))

#### Изменения в `get_context_messages()`:
- Добавлен параметр `user_tier`
- Использует `get_chat_history_limit(tier)` для определения количества сообщений
- Для FREE тарифа (0 сообщений) не загружает историю из БД
- Передает `tier` в `get_system_prompt()` для выбора правильного промпта

#### Изменения в `stream_completion()`:
- Использует `should_save_chat_history(tier)` перед сохранением сообщений
- FREE тариф: сообщения НЕ сохраняются в БД
- Платные тарифы: сообщения сохраняются как обычно

### 4. API чата ([src/api/chat.py](../src/api/chat.py))

Обновлены все эндпоинты для передачи `user_tier`:
- `/chat/stream` - streaming чат
- `/chat/message` - обычный чат
- `/chat/regenerate` - регенерация ответа

Все эндпоинты теперь получают tier из `user.subscription.tier` и передают в `openai_service`.

## Поведение системы

### FREE пользователь:
1. Отправляет сообщение "Что с Bitcoin?"
2. Система НЕ загружает историю (limit=0)
3. AI отвечает без контекста предыдущих сообщений
4. Ответ НЕ сохраняется в БД
5. Следующее сообщение снова без контекста

### BASIC пользователь:
1. Отправляет сообщение "Что с Bitcoin?"
2. Система загружает последние 5 сообщений из истории
3. AI отвечает с учетом контекста
4. Сообщение и ответ сохраняются в БД
5. Может продолжить диалог: "А что с Ethereum?"
6. AI видит предыдущее сообщение про Bitcoin и может сравнить

### PREMIUM/VIP пользователь:
- Аналогично BASIC, но с большим количеством сообщений в памяти (10/50)
- Может вести длительные сложные диалоги
- AI помнит весь контекст разговора

## Работа с чатами

### По умолчанию открывается новый чат
- Когда пользователь открывает приложение, создается новый пустой чат
- Если пользователь пишет сообщение, чат сохраняется (для платных тарифов)

### FREE тариф
- Чаты НЕ сохраняются
- Каждое сообщение независимо
- История не накапливается

### Платные тарифы
- Чаты сохраняются автоматически при первом сообщении
- История накапливается
- Работает как в ChatGPT

## Миграция существующих пользователей

Существующие пользователи:
- FREE: их старая история не удаляется, но НЕ используется в новых запросах
- BASIC/PREMIUM/VIP: продолжают работать с историей как раньше, но с новыми лимитами

## Проверка работы

### Тесты для FREE тарифа:
```python
# 1. Проверить что история не загружается
tier = SubscriptionTier.FREE
limit = get_chat_history_limit(tier)
assert limit == 0

# 2. Проверить что история не сохраняется
should_save = should_save_chat_history(tier)
assert should_save == False
```

### Тесты для платных тарифов:
```python
# 1. Проверить лимиты
assert get_chat_history_limit(SubscriptionTier.BASIC) == 5
assert get_chat_history_limit(SubscriptionTier.PREMIUM) == 10
assert get_chat_history_limit(SubscriptionTier.VIP) == 50

# 2. Проверить сохранение
assert should_save_chat_history(SubscriptionTier.BASIC) == True
assert should_save_chat_history(SubscriptionTier.PREMIUM) == True
assert should_save_chat_history(SubscriptionTier.VIP) == True
```

## Telegram Bot поддержка ✅

Telegram бот использует `openai_service_two_step.py` который ПОЛНОСТЬЮ обновлен под новую систему памяти:
- ✅ Добавлен параметр `user_tier` в `stream_two_step_completion()`
- ✅ Используется `get_chat_history_limit()` для tier-aware загрузки истории
- ✅ Используется `should_save_chat_history()` перед сохранением
- ✅ Bot handlers передают `user_tier` в two_step_service

**Статус:** Реализовано полностью

## Преимущества новой системы

1. **Четкое разделение тарифов**
   - FREE: без памяти → минимальные затраты на tokens
   - BASIC: базовая память → оптимальный баланс
   - PREMIUM/VIP: расширенная память → максимальный UX

2. **Оптимизация затрат**
   - FREE не тратит tokens на историю
   - Платные тарифы используют только нужное количество истории

3. **Улучшенный UX**
   - Промпты адаптированы под возможности тарифа
   - AI не обещает то, что не может сделать
   - Пользователи понимают ограничения своего тарифа

4. **Честная монетизация**
   - Четкая дифференциация ценности
   - Стимул к апгрейду на платные тарифы
   - Справедливое распределение ресурсов

## Файлы изменены

### Backend
- ✅ [config/limits.py](../config/limits.py) - добавлены параметры памяти и helper функции
- ✅ [config/prompts.py](../config/prompts.py) - обновлен промпт для платных тарифов
- ✅ [config/prompts_free.py](../config/prompts_free.py) - обновлен FREE промпт
- ✅ [src/services/openai_service.py](../src/services/openai_service.py) - tier-aware контекст и сохранение (Mini App)
- ✅ [src/services/openai_service_two_step.py](../src/services/openai_service_two_step.py) - tier-aware контекст и сохранение (Telegram Bot)
- ✅ [src/api/chat.py](../src/api/chat.py) - передача user_tier во всех эндпоинтах (Mini App)
- ✅ [src/bot/handlers/chat.py](../src/bot/handlers/chat.py) - передача user_tier (Telegram Bot)

### Модели БД (в процессе)
- 🚧 [src/database/models.py](../src/database/models.py) - добавлены модели Chat и ChatMessage (для множественных чатов)

## Даты реализации

**2025-01-25**
- ✅ Tier-aware память для Mini App
- ✅ Tier-aware память для Telegram Bot
- 🚧 Модели для множественных чатов (в процессе)
