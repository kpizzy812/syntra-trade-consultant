# Стратегия кэширования с Redis для Syntra Trade Consultant

## 📋 Обзор

Интеграция Redis для оптимизации использования бесплатных API и улучшения производительности бота.

## 🎯 Цели

1. **Снижение нагрузки на API** - уменьшить количество запросов к внешним сервисам
2. **Повышение скорости** - быстрые ответы пользователям из кэша
3. **Экономия лимитов** - эффективное использование бесплатных тарифов
4. **Надежность** - сохранение данных между перезапусками бота

## 📊 Анализ API лимитов

### Критические лимиты (требуют агрессивного кэширования)

| API | Лимит | Текущее кэширование | Критичность |
|-----|-------|---------------------|-------------|
| CoinGecko Demo | 30 вызовов/мин<br>10,000/месяц | ✅ In-memory (90-600s) + Rate limiter | 🔴 ВЫСОКАЯ |
| CoinMarketCap | ~333 вызова/день<br>10,000/месяц | ✅ In-memory (300s) | 🔴 КРИТИЧНА|
| DexScreener | 300 вызовов/мин (DEX)<br>60 вызовов/мин (Profile) | ✅ In-memory (300s) | 🟡 СРЕДНЯЯ |

### Некритические лимиты (оптимизация желательна)

| API | Лимит | Текущее кэширование | Критичность |
|-----|-------|---------------------|-------------|
| Binance | 3,000 запросов/мин<br>6,000 weight/мин | ❌ НЕТ | 🟢 НИЗКАЯ |
| CryptoPanic | Нет явных лимитов | ✅ In-memory (300s) | 🟢 НИЗКАЯ |
| Fear & Greed | Публичный API | ❌ НЕТ | 🟢 НИЗКАЯ |

## 🗂️ Стратегия TTL для данных

### Принципы выбора TTL

```
TTL = f(частота_обновления_данных, критичность_лимита_API, актуальность_для_пользователя)
```

### Рекомендуемые TTL по типам данных

| Тип данных | TTL | Обоснование |
|------------|-----|-------------|
| **Цены криптовалют** | 60-90s | Баланс между актуальностью и экономией |
| **Исторические свечи (klines)** | 5-15 мин | Данные не меняются ретроспективно |
| **Данные монет (metadata)** | 1-6 часов | Редко меняется (название, описание) |
| **Рыночная капитализация** | 5 мин | Меняется часто, но не критично |
| **Fear & Greed Index** | 1 час | Обновляется раз в день |
| **Новости (CryptoPanic)** | 15 мин | Новые появляются регулярно |
| **Технические индикаторы** | 5-10 мин | Зависят от свечей |
| **Liquidation data** | 1 час | Исторические данные |
| **Trending coins** | 10-15 мин | Топы меняются медленно |

### Специфичные TTL для эндпоинтов

#### CoinGecko API
```python
CACHE_TTL_COINGECKO = {
    "price": 90,              # /simple/price
    "coin_data": 3600,        # /coins/{id} (metadata)
    "market_data": 300,       # /coins/{id} (market stats)
    "market_chart": 600,      # /coins/{id}/market_chart
    "trending": 900,          # /search/trending
    "global": 300,            # /global
    "search": 1800,           # /search
}
```

#### Binance API
```python
CACHE_TTL_BINANCE = {
    "klines_1m": 60,          # 1-минутные свечи
    "klines_5m": 180,         # 5-минутные свечи
    "klines_15m": 300,        # 15-минутные свечи
    "klines_1h": 600,         # Часовые свечи
    "klines_4h": 1800,        # 4-часовые свечи
    "klines_1d": 3600,        # Дневные свечи
    "current_price": 30,      # Текущая цена
    "funding_rate": 3600,     # Фандинг рейт
    "open_interest": 300,     # Открытый интерес
}
```

#### DexScreener API
```python
CACHE_TTL_DEXSCREENER = {
    "search": 300,            # Поиск токенов
    "token_pairs": 300,       # Пары токена
    "token_price": 60,        # Цена токена
}
```

#### Other APIs
```python
CACHE_TTL_OTHER = {
    "coinmarketcap_quote": 300,     # Котировки CMC
    "cryptopanic_news": 900,        # Новости
    "fear_greed": 3600,             # Fear & Greed Index
}
```

## 🏗️ Архитектура решения

### Выбор библиотеки

**redis-py (v5.0+)** с async поддержкой - официальный клиент Redis

**Почему не aiocache:**
- Избыточный слой абстракции
- redis-py имеет все необходимые функции
- Лучшая производительность
- Активная поддержка

### Структура проекта

```
src/
├── cache/
│   ├── __init__.py
│   ├── redis_manager.py      # Централизованный менеджер Redis
│   ├── cache_keys.py          # Генераторы ключей кэша
│   └── cache_decorators.py    # Декораторы для кэширования
├── services/
│   ├── binance_service.py     # + Redis кэширование
│   ├── coingecko_service.py   # Миграция на Redis
│   ├── dexscreener_service.py # Миграция на Redis
│   └── ...
└── config/
    └── cache_config.py        # Конфигурация TTL
```

### Схема ключей Redis

```
Формат: {namespace}:{service}:{method}:{params_hash}

Примеры:
- syntra:coingecko:price:bitcoin_usd
- syntra:binance:klines:BTCUSDT_1h_100
- syntra:dexscreener:search:bitcoin
- syntra:feargreed:current
```

**Преимущества:**
- Легко идентифицировать источник
- Избегание коллизий
- Простота отладки
- Возможность массового удаления по паттерну

## 🔧 Технические детали

### Connection Pool

```python
from redis.asyncio import Redis, ConnectionPool

# Глобальный connection pool
pool = ConnectionPool(
    host="localhost",
    port=6379,
    db=0,
    max_connections=50,
    decode_responses=True,
    socket_connect_timeout=5,
    socket_timeout=5,
)

redis_client = Redis(connection_pool=pool)
```

### Graceful Degradation

Бот должен работать если Redis недоступен:

```python
async def get_cached_or_fetch(key, fetch_func, ttl):
    try:
        # Попытка получить из Redis
        cached = await redis_client.get(key)
        if cached:
            return json.loads(cached)
    except Exception as e:
        logger.warning(f"Redis unavailable: {e}")

    # Fallback к прямому запросу
    data = await fetch_func()

    # Попытка сохранить в кэш (не критично если упадет)
    try:
        await redis_client.setex(key, ttl, json.dumps(data))
    except Exception as e:
        logger.warning(f"Failed to cache: {e}")

    return data
```

### Serialization

- **JSON** для большинства данных (читаемость, совместимость)
- **MessagePack** для больших объемов (опционально, для оптимизации)

## 📈 Мониторинг и метрики

### Ключевые метрики

1. **Cache Hit Rate** - % запросов из кэша
2. **API Calls Saved** - количество сэкономленных запросов
3. **Response Time** - улучшение скорости ответа
4. **Memory Usage** - использование памяти Redis

### Logging

```python
logger.info(f"Cache HIT: {key}")
logger.info(f"Cache MISS: {key} - fetching from API")
logger.warning(f"Cache EXPIRED: {key} - TTL was {ttl}s")
logger.error(f"Cache ERROR: {key} - {error}")
```

## 🚀 План внедрения

### Фаза 1: Инфраструктура (День 1)
- ✅ Установить Redis
- ✅ Создать `RedisManager` с connection pool
- ✅ Создать утилиты для работы с ключами
- ✅ Добавить декораторы кэширования
- ✅ Настроить конфигурацию TTL

### Фаза 2: Миграция сервисов (День 2-3)
- ✅ Мигрировать Binance Service (приоритет - нет кэша)
- ✅ Мигрировать Fear & Greed Service
- ✅ Мигрировать CoinGecko Service (заменить in-memory на Redis)
- ✅ Мигрировать DexScreener Service
- ✅ Мигрировать CoinMarketCap Service
- ✅ Мигрировать CryptoPanic Service

### Фаза 3: Тестирование (День 4)
- ✅ Unit тесты для RedisManager
- ✅ Integration тесты для сервисов
- ✅ Тесты graceful degradation (Redis недоступен)
- ✅ Load testing

### Фаза 4: Мониторинг (День 5)
- ✅ Добавить метрики кэша
- ✅ Dashboard для мониторинга
- ✅ Alerting при проблемах с Redis

## 💡 Best Practices

1. **Всегда используй TTL** - избегай неограниченного роста памяти
2. **Namespace ключей** - избегай коллизий между сервисами
3. **Connection pooling** - переиспользуй соединения
4. **Graceful degradation** - бот работает без Redis
5. **Логирование** - отслеживай cache hit/miss
6. **Мониторинг** - следи за метриками
7. **Тестирование** - проверяй работу кэша

## 🔐 Безопасность

1. **Redis password** - защити Redis паролем в production
2. **Network isolation** - Redis за firewall
3. **Не кэшируй чувствительные данные** - API ключи, токены пользователей
4. **Rotate keys** - периодически очищай старые ключи

## 📦 Зависимости

```txt
redis>=5.0.0
```

## 🎓 Ресурсы

- [redis-py Documentation](https://redis.readthedocs.io/)
- [Redis Best Practices](https://redis.io/docs/manual/patterns/)
- [Async Python with Redis](https://redis.io/learn/develop/python/fastapi)

---

**Автор**: Claude Code
**Дата**: 2025-01-19
**Статус**: В разработке
