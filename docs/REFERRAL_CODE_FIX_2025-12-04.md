# Исправление: Автоматическая генерация referral_code при регистрации

**Дата**: 2025-12-04
**Проблема**: Новые пользователи, заходящие через `?startapp` (напрямую в Mini App), не получали `referral_code` при создании аккаунта, что вызывало ошибки на страницах `/home`, `/referral`, `/profile`.

## 🐛 Описание проблемы

Когда новый пользователь заходил напрямую в Mini App через `?startapp` (без предварительного взаимодействия с ботом), функция `get_or_create_user` создавала пользователя с `telegram_id`, но **не генерировала** `referral_code`.

### Почему это критично:

1. **Реферальная система** - все эндпоинты ожидают наличие `referral_code` у пользователя
2. **API ошибки** - страницы `/home`, `/referral`, `/profile` падали с ошибками из-за `None` вместо реферального кода
3. **Inconsistent state** - пользователь создан, но функционал реферралов не работает

## ✅ Решение

Обновлены **две функции** создания пользователей для автоматической генерации `referral_code`:

### 1. `create_user()` - для Telegram пользователей

**Файл**: [`src/database/crud.py:93-144`](src/database/crud.py#L93-L144)

**Изменения**:
```python
# Добавлено после создания пользователя:
# Generate unique referral code for new user
user.referral_code = await generate_referral_code(session, user.id)
await session.commit()
```

**Теперь происходит**:
1. Создается User в БД
2. Генерируется уникальный 8-символьный `referral_code`
3. Сохраняется в БД
4. Создается trial подписка
5. Логируется с информацией о реферальном коде

### 2. `create_user_from_email()` - для Web пользователей

**Файл**: [`src/database/crud.py:3453-3504`](src/database/crud.py#L3453-L3504)

**Изменения**:
```python
# Добавлено после создания пользователя:
# Generate unique referral code for new user
user.referral_code = await generate_referral_code(session, user.id)
await session.commit()
```

**Теперь происходит**:
1. Создается User в БД (email verified)
2. Генерируется уникальный `referral_code`
3. Сохраняется в БД
4. Создается trial подписка (если не заблокирован fraud detection)
5. Логируется с информацией о реферальном коде

## 🔄 Сценарии регистрации

### ✅ Сценарий 1: /start в боте
- Пользователь отправляет `/start` боту
- Middleware вызывает `get_or_create_user()` → `create_user()`
- **Генерируется `referral_code`** ✅
- Создается trial подписка
- Работает корректно

### ✅ Сценарий 2: Magic Link (Web)
- Пользователь вводит email
- Переходит по magic link
- Вызывается `get_or_create_user_from_email()` → `create_user_from_email()`
- **Генерируется `referral_code`** ✅
- Создается trial подписка
- Работает корректно

### ✅ Сценарий 3: Прямой вход через ?startapp (Mini App)
- Новый пользователь открывает Mini App напрямую
- Telegram отправляет `initData` с user info
- Вызывается `get_or_create_user()` → `create_user()`
- **Генерируется `referral_code`** ✅
- Создается trial подписка
- Страницы `/home`, `/referral`, `/profile` работают корректно

## 🧪 Как проверить

### 1. Создать нового пользователя через Mini App:
```bash
# Открыть Mini App через t.me/YourBot/app?startapp=test
# Проверить что пользователь создался с referral_code
```

### 2. Проверить в базе данных:
```sql
SELECT id, telegram_id, username, referral_code, created_at
FROM users
WHERE referral_code IS NOT NULL
ORDER BY created_at DESC
LIMIT 10;
```

### 3. Проверить логи:
```bash
grep "referral_code:" logs/bot.log | tail -5
```

Должны увидеть:
```
User created: 123456 (@username) with language: ru, referral_code: ABC12XYZ, 7-day PREMIUM trial activated
```

## 📝 Затронутые файлы

1. **[`src/database/crud.py`](src/database/crud.py)**:
   - `create_user()` - добавлена генерация `referral_code`
   - `create_user_from_email()` - добавлена генерация `referral_code`

2. **[`src/api/auth.py`](src/api/auth.py)**:
   - `get_current_user()` - использует `get_or_create_user()` (теперь с автоматической генерацией кода)

## 🚀 Деплой

После обновления кода:

1. **Перезапустить бота**:
```bash
./manage.sh restart bot
```

2. **Перезапустить API**:
```bash
./manage.sh restart api
```

3. **Проверить существующих пользователей без referral_code**:
```sql
-- Найти пользователей без реферального кода
SELECT COUNT(*) FROM users WHERE referral_code IS NULL;

-- Если есть, можно добавить миграцию для генерации кодов
```

## ✅ Миграция существующих пользователей

Для существующих пользователей без `referral_code` создан специальный скрипт:

### Запуск скрипта:

```bash
source .venv/bin/activate
python scripts/fix_missing_referral_codes.py
```

**Файлы**:
- [`scripts/fix_missing_referral_codes.py`](scripts/fix_missing_referral_codes.py) - Python скрипт (рекомендуется)
- [`alembic/versions/fix_missing_referral_codes.py`](alembic/versions/fix_missing_referral_codes.py) - Alembic миграция (альтернатива)

### Что делает скрипт:

1. Находит всех пользователей без `referral_code`
2. Генерирует уникальный 8-символьный код для каждого
3. Сохраняет в БД
4. Логирует результаты

### Проверка:

```bash
# Проверить статус пользователей
python -c "
import asyncio
from sqlalchemy import select, func
from src.database.engine import get_session_maker
from src.database.models import User

async def check():
    session_maker = get_session_maker()
    async with session_maker() as session:
        stmt = select(func.count(User.id)).where(User.referral_code.is_(None))
        result = await session.execute(stmt)
        without_code = result.scalar()
        print(f'Users without referral_code: {without_code}')

asyncio.run(check())
"
```

**Статус**: ✅ Все пользователи имеют referral codes (проверено 2025-12-04)

## 📊 Impact

- **Затронутые пользователи**: Новые пользователи, заходящие через Mini App
- **Breaking changes**: Нет
- **Database changes**: Нет (только заполнение существующего поля)
- **API changes**: Нет
- **Frontend changes**: Нет

## ✨ Результат

Теперь **все новые пользователи** автоматически получают уникальный `referral_code` при создании аккаунта, независимо от способа регистрации:

- ✅ Telegram Bot (`/start`)
- ✅ Web (Magic Link)
- ✅ Mini App (direct `?startapp`)

**Проблема полностью решена!** 🎉
